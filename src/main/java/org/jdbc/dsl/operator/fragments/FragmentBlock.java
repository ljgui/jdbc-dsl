package org.jdbc.dsl.operator.fragments;

public enum FragmentBlock {
    before,
    selectColumn,
    selectFrom,
    join,
    where,
    term,
    groupBy,
    orderBy,
    having,
    paging,
    other,
    after,
}
